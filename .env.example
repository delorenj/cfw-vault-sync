# Cloudflare R2 Worker Sync Configuration

# The secret token for authenticating with your worker
# This should match the value you set with: npx wrangler secret put SYNC_TOKEN
SYNC_TOKEN=your-secret-token-here

# Path to your Obsidian vault or documents folder
VAULT_PATH=/home/delorenj/code/DeLoDocs

# Worker URL for production (replace with your deployed worker URL)
WORKER_URL=https://r2-worker.jaradd.workers.dev

# Worker URL for local development
# WORKER_URL=http://localhost:8787